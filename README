This repository contains ScisoftPy scripts for processing high resolution powder
diffraction (HRPD) data.

They are aimed for use with I11's data files to sum and re-bin overlapping scans.

To install, use
    $ pip install hrpdrebin[pyqt] # or replace with [pyside], [pyqt6], or [pyside6]
